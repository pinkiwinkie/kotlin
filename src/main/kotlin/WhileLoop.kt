fun main() {
    val brand = arrayOf("A", "m", "i")
    var index = 0
    while (index <= brand.size - 1) {
        println(brand[index])
        ++index
    }

    //1. Write a loop that prints 0 to 9 on the screen using while
    var i = 0
    while (i < 10 ) {
        println(i)
        i++
    }
}